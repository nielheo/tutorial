const memberFacebookType = `
  type MemberFacebook {
    id: Int!,
    name: String!,
    email: String!,
    accessToken: String!,
    userId: String!,
    signedRequest: String!
  }
`;

export default memberFacebookType;
